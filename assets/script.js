
const menuBtn=document.querySelector('.hamb');const menu=document.querySelector('.menu');if(menuBtn&&menu){menuBtn.addEventListener('click',()=>menu.classList.toggle('open'));menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>menu.classList.remove('open')))}
const reviewsKey='am-shopping-gate-reviews-v1';
function loadReviews(){const list=document.querySelector('#reviewList');if(!list)return;let rs=[];try{rs=JSON.parse(localStorage.getItem(reviewsKey)||'[]')}catch(e){};rs.slice(-6).reverse().forEach(r=>{const el=document.createElement('article');el.className='review';el.innerHTML=`<div class="stars">${'★'.repeat(Math.max(1,Math.min(5,Number(r.rating)||5)))}</div><p>${escapeHtml(r.text)}</p><b>${escapeHtml(r.name)}</b>`;list.prepend(el)})}
function escapeHtml(s){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
const rf=document.querySelector('#reviewForm');if(rf){rf.addEventListener('submit',e=>{e.preventDefault();const fd=new FormData(rf);const r={name:fd.get('name'),rating:fd.get('rating'),text:fd.get('text')};let rs=[];try{rs=JSON.parse(localStorage.getItem(reviewsKey)||'[]')}catch(e){}rs.push(r);localStorage.setItem(reviewsKey,JSON.stringify(rs));rf.reset();document.querySelector('#reviewMsg').textContent='نظر شما با موفقیت در همین مرورگر ثبت شد. برای نمایش عمومی نیاز به اتصال پایگاه داده/سرویس فرم دارید.';loadReviews()})}loadReviews();
